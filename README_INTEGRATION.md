# 🎨 Интеграция динамических цветов смен

## Быстрый старт

### Автоматическая интеграция (рекомендуется)

```bash
# 1. Перейдите в репозиторий
cd sm1

# 2. Переключитесь на ветку
git checkout feature/corporate-branding
git pull origin feature/corporate-branding

# 3. Восстановите app.js
bash RESTORE_AND_INTEGRATE.sh

# 4. Примените патчи автоматически
node integrate-colors.js

# 5. Проверьте изменения
git diff js/app.js

# 6. Закоммитьте
git add js/app.js
git commit -m "feat: Complete integration of dynamic shift colors"
git push origin feature/corporate-branding
```

---

## Ручная интеграция

Если автоматический скрипт не работает, следуйте инструкции в **[INTEGRATION_TASK.md](./INTEGRATION_TASK.md)**

---

## Что было сделано

### ✅ Готовые компоненты

1. **js/shift-colors.js** - Модуль управления цветами
   - HSL цветовое пространство с golden ratio
   - Детерминированное распределение цветов
   - Автоматическая адаптация под тему
   - Раздельные палитры для L1 и L2

2. **css/styles.css** - Стили для цветных пилюль
   - CSS-классы для 20 цветов
   - Адаптация под светлую/темную тему
   - Специальный стиль для ВЫХ/ОТП/ДР
   - Стили для легенды

3. **index.html** - Контейнер легенды
   - Блок `<div id="shift-legend-root">`
   - Подключение модуля shift-colors.js

### ⚙️ Интеграция в app.js

Скрипт `integrate-colors.js` автоматически применяет 5 изменений:

1. **Функция renderShiftLegend()** - отрисовка легенды смен
2. **Инициализация в loadShiftsCatalog()** - запуск системы цветов
3. **Обновление в applyTheme()** - адаптация цветов под тему
4. **Применение цветов в renderScheduleCurrentLine()** - раскраска пилюль
5. **Обновление легенды** - при переключении L1/L2

---

## Результат

### До интеграции
- ❌ Все смены одинакового цвета (серые)
- ❌ Сложно различать типы смен
- ❌ Нет легенды

### После интеграции
- ✅ Каждый шаблон смены имеет уникальный цвет
- ✅ Специальные смены (ВЫХ, ОТП, ДР) выделены желтым
- ✅ Легенда внизу экрана показывает все смены
- ✅ Цвета адаптируются под светлую/темную тему
- ✅ Разные палитры для L1 и L2
- ✅ Детерминированность - один templateId = один цвет

---

## Проверка работы

После интеграции откройте приложение в браузере:

1. **Проверьте таблицу смен:**
   - Каждый тип смены должен иметь свой цвет
   - Специальные смены (ВЫХ, ОТП, ДР) должны быть желтыми

2. **Проверьте легенду:**
   - Внизу экрана должна появиться легенда
   - При переключении L1/L2 легенда должна обновляться

3. **Проверьте смену темы:**
   - Нажмите кнопку смены темы (🌙/☀️)
   - Цвета должны корректно адаптироваться

4. **Проверьте консоль браузера:**
   - Не должно быть ошибок
   - Должны быть логи инициализации ShiftColors

---

## Технические детали

### Система цветов

- **Алгоритм:** Golden ratio в HSL пространстве
- **Формула:** `hue = (index * 137.5) % 360`
- **Палитра:** 20 уникальных цветов на линию
- **Адаптация:** Автоматическое изменение saturation/lightness

### Детерминированность

Каждый `templateId` всегда получает один и тот же цвет:
- `templateId 123` → всегда `shift-color-3`
- `templateId 456` → всегда `shift-color-6`

Это гарантирует консистентность отображения.

### Доступность

- Контраст > 4.5:1 для обычного текста
- Контраст > 3:1 для крупного текста
- Поддержка клавиатурной навигации
- Семантическая разметка

---

## Структура файлов

```
sm1/
├── js/
│   ├── app.js                    # Основной файл приложения
│   ├── shift-colors.js           # Модуль цветов ✅
│   └── app-patch.js              # Блоки кода для ручной интеграции
├── css/
│   └── styles.css                # Стили с цветными классами ✅
├── index.html                     # HTML с блоком легенды ✅
├── INTEGRATION_TASK.md           # Пошаговая инструкция
├── INTEGRATION_GUIDE.md          # Общее описание
├── RESTORE_AND_INTEGRATE.sh      # Bash скрипт восстановления
├── integrate-colors.js           # Node.js скрипт интеграции
└── README_INTEGRATION.md         # Этот файл
```

---

## Решение проблем

### Ошибка: "ShiftColors is not defined"

**Причина:** Модуль shift-colors.js не подключен в HTML

**Решение:**
```html
<script src="js/shift-colors.js"></script>
<script src="js/app.js"></script>
```

### Цвета не применяются

**Причина:** Не вызвана инициализация или нет templateId в смене

**Проверьте:**
1. `ShiftColors.initialize()` вызывается в `loadShiftsCatalog()`
2. У смен есть поле `shift.templateId`
3. Нет ошибок в консоли браузера

### Легенда не отображается

**Причина:** Блок `shift-legend-root` отсутствует или функция не вызывается

**Проверьте:**
1. В index.html есть `<div id="shift-legend-root"></div>`
2. `renderShiftLegend()` вызывается после загрузки данных
3. CSS стили для `.shift-legend-*` присутствуют

### Легенда не обновляется при переключении L1/L2

**Причина:** Не добавлен вызов `renderShiftLegend()` в обработчики

**Решение:** Добавьте `renderShiftLegend();` после `renderScheduleCurrentLine();` в обработчиках btnLineL1El и btnLineL2El

---

## Откат изменений

Если что-то пошло не так:

```bash
# Откатить app.js к состоянию до интеграции
git checkout HEAD~1 -- js/app.js

# Или откатить весь коммит
git reset --hard HEAD~1

# Затем можно попробовать снова
bash RESTORE_AND_INTEGRATE.sh
node integrate-colors.js
```

---

## Контакты

Если возникли проблемы:
1. Проверьте [INTEGRATION_TASK.md](./INTEGRATION_TASK.md)
2. Проверьте консоль браузера на ошибки
3. Проверьте логи скрипта `integrate-colors.js`
4. Создайте issue в репозитории

---

**Успешной интеграции! 🚀**
